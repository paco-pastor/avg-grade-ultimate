FROM mono:6.12.0.182-slim

WORKDIR /app

RUN apt update
RUN apt install -y mono-msc

COPY main.cs .
RUN csc main.cs
RUN csc -r:Newtonsoft.Json.dll -r:CsvHelper.dll main.cs
# TODO add dll to container

CMD ["sleep", "infinity"]